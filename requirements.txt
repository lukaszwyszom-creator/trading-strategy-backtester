# Minimalne zaleznosci zewnetrzne dla Python 3.11 na Synology
# Moduly standard library (argparse, dataclasses, json, logging, pathlib, typing, itertools)
# nie wymagaja instalacji przez pip.

numpy>=1.26,<3
pandas>=2.1,<3
yfinance>=0.2.54,<0.3
ta>=0.11,<0.12
